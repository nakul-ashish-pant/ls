@artifact.package@class @artifact.name@ implements Serializable {

    //static transients = []

    //static hasMany=[]

    //static belongsTo=[]

    static constraints = {
    }

    static mapping = {
    }
}